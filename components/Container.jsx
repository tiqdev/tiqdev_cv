const Container = ({ children }) => (
  <div className="w-full max-w-[1200px] mx-auto flex flex-col items-center- justify-center md:mt-[0px] mt-[40px] relative z-10">
    {children}
  </div>
);

export default Container;
